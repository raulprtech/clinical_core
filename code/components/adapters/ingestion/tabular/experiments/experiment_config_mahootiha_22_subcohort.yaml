# ============================================================
# CLINICAL-CORE / TABULAR-IN — Mahootiha replication on 22 features (LEAKAGE), subcohort n≈224
# ============================================================
# Variante del experiment_config_mahootiha_22.yaml con cohort_filter activo.
# Aplica Mahootiha's ranking + Cox sobre el subcohort MMEM-comparable con TODAS
# las 22 features (incluye karnofsky/ecog/tumor_status). Replica más fiel del
# entorno bajo el que ellos reportaron 0.84.
# ============================================================

experiment:
  name: "mahootiha_22_with_leakage_subcohort_dfs5mod"
  description: "Mahootiha 2024 feature ranking + Cox over top-K, 22 features (with leakage), MMEM-comparable subcohort."
  author: "Raul Pacheco"
  protocol_version: "v8.1"

data:
  xml_dir: "/home/raulprtech/clinical_core/data/raw/clinicalsupplement"
  feature_config: "../configs/tabular_mapping_22_with_leakage.yaml"
  drug_file: null

output:
  base_dir: "/home/raulprtech/clinical_core/results"
  save_raw_extraction: true
  save_resolved_config: true

random:
  seeds: [42, 123, 456, 789, 1024]
  n_folds: 5

cohort_filter:
  enabled: true
  require_dfs_valid: true
  modality_manifest_path: "/home/raulprtech/clinical_core/data/manifests/gdc_modality_manifest_TCGA-KIRC_20260528.csv"
  require_modalities: [wsi, mrna_seq, mirna_seq, methylation_450k]

phase_1_imputation:          { enabled: false }
phase_2_variants:            { enabled: false }
phase_2_holdout:             { enabled: false }
phase_2_external_baselines:  { enabled: false }
phase_2_external_holdout:    { enabled: false }
phase_3_efficiency:          { enabled: false }
phase_4_stress:              { enabled: false }
phase_5_multimodal:          { enabled: false }
phase_6_fusion_proc:         { enabled: false }
phase_7_turbolatent:         { enabled: false }
phase_8_prognosis_benchmark: { enabled: false }

phase_2_mahootiha:
  enabled: true
  holdout_fraction: 0.20
  seeds: [42, 123, 456, 789, 1024]
  k_values: [5, 10, 15, null]
  imputation: "knn_5"
  rf_n_estimators: 200
  output_dim: 768

runtime:
  verbosity: "normal"
  fail_fast: false
